apiVersion: apps/v1
kind: Deployment
metadata:
  name: pamawas-correlator
  namespace: pamawas
  labels:
    app: pamawas-correlator
    app.kubernetes.io/name: pamawas-correlator
    app.kubernetes.io/part-of: pamawas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pamawas-correlator
  template:
    metadata:
      labels:
        app: pamawas-correlator
        app.kubernetes.io/name: pamawas-correlator
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: pamawas
      containers:
        - name: pamawas-correlator
          image: ghcr.io/pamawas/pamawas-correlator:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: pamawas-secrets
                  key: DATABASE_URL
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: pamawas-config
                  key: PORT
            - name: CORRELATION_TIME_WINDOW
              valueFrom:
                configMapKeyRef:
                  name: pamawas-config
                  key: CORRELATION_TIME_WINDOW
            - name: CORRELATION_INTERVAL
              valueFrom:
                configMapKeyRef:
                  name: pamawas-config
                  key: CORRELATION_INTERVAL
            - name: CORRELATOR_MODE
              valueFrom:
                configMapKeyRef:
                  name: pamawas-config
                  key: CORRELATOR_MODE
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: pamawas-config
                  key: LOG_LEVEL
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: pamawas-config
                  key: ENVIRONMENT
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: pamawas-config
                  key: OTEL_EXPORTER_OTLP_ENDPOINT
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"